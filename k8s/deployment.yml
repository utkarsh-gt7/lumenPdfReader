apiVersion: apps/v1
kind: Deployment
metadata:
  name: pdf-reader
  labels:
    app: pdf-reader
spec:
  replicas: 2
  selector:
    matchLabels:
      app: pdf-reader
  template:
    metadata:
      labels:
        app: pdf-reader
    spec:
      containers:
        - name: pdf-reader
          image: ghcr.io/your-username/pdf-reader:latest
          ports:
            - containerPort: 80
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 250m
              memory: 256Mi
          livenessProbe:
            httpGet:
              path: /
              port: 80
            initialDelaySeconds: 10
            periodSeconds: 30
          readinessProbe:
            httpGet:
              path: /
              port: 80
            initialDelaySeconds: 5
            periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: pdf-reader-service
spec:
  selector:
    app: pdf-reader
  ports:
    - port: 80
      targetPort: 80
  type: LoadBalancer
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: pdf-reader-ingress
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  rules:
    - host: lumen.yourdomain.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: pdf-reader-service
                port:
                  number: 80
